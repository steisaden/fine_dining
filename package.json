{
  "name": "esker-final-millimeter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "go run ./cmd/server -generate public/generated && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "7.3.6"
  }
}
